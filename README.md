# api-server

Internal API service. Handles authentication, webhook routing, and deployments.

## Setup

```bash
git clone git@github.com:akanseoluwa/Agentic.git
bundle install && npm install
cp .env.example .env
rails db:create db:migrate
bin/dev
```
